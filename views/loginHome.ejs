<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1>Congrats u logged in!</h1>
    <a href="/loginHome/folder">Create a new folder</a>

    <h2>Your Folders:</h2>
    <% if (folders.length === 0) { %>
      <p>You have no folders yet.</p>
    <% } else { %>
      <ul>
        <% folders.forEach(folder => { %>
          <li>
          <a href="/loginHome/folder/<%= folder.id %>"><%= folder.name %></a>
          <a href="/loginHome/edit/<%= folder.id %>">Edit</a>
          <a href="/loginHome/delete/<%= folder.id %>" onclick="return confirm('Are you sure you want to delete this message?');">Delete</a>
          </li>
        <% }) %>
      </ul>
    <% } %>
    
    <a href="/logout">Logout</a>
</body>
</html>